in instruction: grgd
in function: fds
in line: sgf
in column: gd

define last(s) := last(s,s)

define eventCount(self, e) :=
  default(last(self, e) + 1, 0)

define function_call(name) :=
  if function == name && last(function) != name then ()

define f_count := eventCount(f_count, function_call("f"))

out f_count