in a: Events<Int>
in b: Events<Int>

def div := a / b
def mult := a * b
def sum := a + b
def sub := a - b
def diff := if (a < b) then b - a else a - b
def defined(a) := default(const(true, a), false)

out defined(a)
out *
out a * b