in instruction: grgd
in function: fds
in line: sgf
in column: gd

def last(s) := last(s,s)

def eventCount(e) := {
  def ec := default(last(ec, e) + 1, 0)
  ec
}

def function_call(name) :=
  if function == name && last(function) != name then ()

def f_count := eventCount(function_call("f"))

out f_count