in map: Events[Map[Int, {x: Int, y: Int}]]

def list = Map_fold(map, List_empty[Int], (acc: List[Int], key: Int, value: {x: Int, y: Int}) => List_append(acc, key * value.x * value.y))
out list
out Map_keys(map) as keys
