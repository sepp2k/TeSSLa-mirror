in x: Events[Int]

def seen: Events[Set[Int]] := default(Set_add(last(seen, x), x), Set.empty[Int])

def newOrOld :=
    if Set_contains(last(seen, seen), x)
    then "old"
    else "new"

def size := Set_size(seen)

out *