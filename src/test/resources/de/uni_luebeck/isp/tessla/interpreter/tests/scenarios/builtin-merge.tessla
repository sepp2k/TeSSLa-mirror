in x: Events[Int]
in y: Events[Int]
in z: Events[Int]

def mergeUnit[T,U](x: Events[T], y: Events[U]): Events[Unit] :=
  merge(const((), x), const((), y))

def a := mergeUnit(x,y)
def b := merge(x,y)

out a
out b
